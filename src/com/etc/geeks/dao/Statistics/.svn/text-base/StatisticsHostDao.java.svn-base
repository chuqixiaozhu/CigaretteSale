package com.etc.geeks.dao.Statistics;

import java.util.List;

import com.etc.geeks.entity.Statistics.StatisticsHost;

public interface StatisticsHostDao {
	/**
	 * 增加一个统计主表表单
	 * @param sh
	 * @return
	 */
	public abstract int addStatisticsHost(StatisticsHost sh);
	/**
	 * 根据表单号删除一个统计主表表单信息
	 * @param statisticHostId
	 * @return
	 */
	public abstract int deleteById(String statisticHostId);
	
	/**
	 * 更新一个统计主表表单信息
	 * @param sh
	 * @return
	 */
	public abstract int updateStatisticHost(StatisticsHost sh);
	
	/**
	 * 根据表单ID查找一个统计主表表单信息
	 * @param statisticsHostId
	 * @return
	 */
	public abstract StatisticsHost findById(String statisticsHostId);
	
	/**
	 * 查找所有统计主表表单信息
	 * @return
	 */
	public abstract List<StatisticsHost> findAll();
}
